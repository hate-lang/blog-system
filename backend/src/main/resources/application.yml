server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # ????????????? my_blog ????
    url: jdbc:mysql://localhost:3306/my_blog?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 1234                    # ??????部署服务器后需要改为服务器上MySQL的密码
    #  新增：文件上传大小配置
  servlet:
    multipart:
      max-file-size: 10MB  # 单个文件最大允许 10MB
      max-request-size: 50MB  # 单次请求总大小最大允许 50MB


# MyBatis-Plus ???????????? SQL ????????????????
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
